/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
	i18n: {
		// Locales: ['en', 'it'],
		locales: ['it'],
		defaultLocale: 'it',
	},
};

module.exports = nextConfig;
