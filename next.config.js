/** @type {import('next').NextConfig} */
const nextConfig = {
	output: 'standalone',
	reactStrictMode: true,
	// I18n: {
	// 	locales: ['it'],
	// 	// Locales: ['it', 'en'],
	// 	defaultLocale: 'it',
	// },
};

module.exports = nextConfig;
